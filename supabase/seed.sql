-- Seed de demonstração (APENAS desenvolvimento / DEMO_MODE)
-- Não execute em produção com dados reais de usuários.
-- Os IDs abaixo são fixos para testes determinísticos.

-- Nota: profiles normalmente nascem de auth.users.
-- Para demo sem Auth, use o script backend `npm run seed:demo`
-- que cria usuários via Admin API do Supabase quando DEMO_MODE=true.

-- Configurações iniciais já estão em app_settings na migration.
